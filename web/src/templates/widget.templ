package templates

templ widgetTitle() {
    <p class="text-2xl font-bold">
        { children... }
    </p>
}

templ widget() {
    <div class="p-4 bg-neutral-800 border-1 border-neutral-500 rounded-lg flex flex-col gap-4">
        { children... }
    </div>
}